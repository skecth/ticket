<p style="color: green"><%= notice %></p>

<%= link_to "Organizer Sign Up", new_organizer_registration_path %>
abc

<h1>Events</h1>
<h2> try to add a new event </h2>

<div id="events">
  <% @events.each do |event| %>
    <%= render event %>
    <p>
      <%= link_to "Show this event", event %>
    </p>
  <% end %>
</div>

<%= link_to "New event", new_event_path %>
